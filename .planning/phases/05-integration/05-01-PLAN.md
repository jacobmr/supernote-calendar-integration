---
phase: 05-integration
plan: 01
type: execute
---

<objective>
Create integration test infrastructure and verify the complete recurring meeting pipeline from detection through note creation.

Purpose: Prove the full recurring meeting flow works end-to-end with real service interactions (mocked external APIs only).
Output: Integration test suite covering recurring meetings: folder creation, note upload, and deduplication.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph:

@.planning/phases/04-note-templates/04-02-SUMMARY.md
@.planning/phases/03-folder-organization/03-02-SUMMARY.md

# Key source files (the services being integration-tested):

@src/services/meeting-detector.ts
@src/services/folder-organizer.ts
@src/services/note-creator.ts
@src/services/note-template-generator.ts
@src/services/folder-mapping-store.ts
@src/services/supernote-api.ts

# Existing test patterns:

@tests/**mocks**/supernote-cloud-api.js
@jest.config.js

**Tech stack available:** Jest, ts-jest, supernote-cloud-api mock
**Established patterns:** Service class composition, FolderMappingStore JSON persistence, 500ms rate limiting, per-meeting error isolation, graceful degradation
**Constraining decisions:**

- [Phase 3]: RecurringFolderMapping keyed by recurringEventId — all instances share one folder
- [Phase 4]: Recurring filenames omit time (YYYY-MM-DD Title.md), duplicate prevention via noteFileName field
- [Phase 4]: NoteCreator reuses same SupernoteAPIClient as FolderOrganizer
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test helpers and mock factories</name>
  <files>tests/integration/pipeline-helpers.ts</files>
  <action>
Create a shared test helper module for pipeline integration tests:

1. **MeetingData factory functions:**
   - `createRecurringMeeting(overrides?)` — returns MeetingData with isRecurring=true, recurringEventId set, reasonable defaults (title, attendees, start/end times)
   - `createAdHocMeeting(overrides?)` — returns MeetingData with isRecurring=false, no recurringEventId
   - `createRecurringInstance(recurringEventId, instanceNum, overrides?)` — creates a specific instance of a recurring series (different id but same recurringEventId)
   - Use incrementing counters for unique IDs to avoid collisions between tests

2. **Mock SupernoteAPIClient factory:**
   - `createMockSupernoteClient()` — returns a mock SupernoteAPIClient where:
     - `authenticate()` resolves successfully
     - `createFolderPath(path)` resolves to a deterministic folder ID (hash of path string, or incrementing number)
     - `uploadTextFile(content, filename, folderId)` resolves successfully and records calls for assertion
   - Expose `getCalls()` to inspect what was uploaded (filename, folderId, content)

3. **Temp FolderMappingStore factory:**
   - `createTempMappingStore()` — creates FolderMappingStore with a temp file path (use `os.tmpdir()` + random suffix)
   - Returns both the store instance and a cleanup function

Export all helpers. Do NOT use jest.mock() for the SupernoteAPIClient in these helpers — create manual mock objects that implement the same interface. This allows integration tests to verify real service interactions with controllable external boundaries.
</action>
<verify>npx tsc --noEmit tests/integration/pipeline-helpers.ts (types check)</verify>
<done>Helper module exports factory functions for MeetingData, mock SupernoteAPIClient, and temp FolderMappingStore — all type-safe</done>
</task>

<task type="auto">
  <name>Task 2: Integration test for recurring meeting pipeline</name>
  <files>tests/integration/recurring-pipeline.test.ts</files>
  <action>
Create integration test file that tests the full recurring meeting pipeline using the helpers from Task 1.

**Test cases:**

1. **"should create folder and note for a new recurring meeting"**
   - Create a recurring meeting via factory
   - Instantiate FolderOrganizer with mock client + temp mapping store
   - Call `folderOrganizer.processNewMeetings([meeting])`
   - Verify: createFolderPath called with `Calendar/Recurring` and `Calendar/Ad-Hoc` (base structure) + `Calendar/Recurring/[sanitized title]`
   - Verify: FolderMappingStore has mapping for this meeting AND recurringFolder entry
   - Instantiate NoteCreator with same mock client + NoteTemplateGenerator + same mapping store
   - Call `noteCreator.processNewMeetings([meeting])`
   - Verify: uploadTextFile called once with filename matching `YYYY-MM-DD Title.md` pattern
   - Verify: uploaded content contains meeting title, attendees section, action items section
   - Verify: FolderMappingStore mapping now has noteFileName set

2. **"should reuse folder for second instance of same recurring event"**
   - Create two instances of same recurring event (same recurringEventId, different ids)
   - Process first instance through FolderOrganizer → creates folder
   - Process second instance through FolderOrganizer → reuses folder (no new createFolderPath for meeting subfolder)
   - Verify: both instances mapped to same folderId
   - Process both through NoteCreator
   - Verify: uploadTextFile called twice (one note per instance, different filenames)

3. **"should not re-create note on second run (deduplication)"**
   - Process a recurring meeting through full pipeline (folder + note)
   - Reset mock client call tracking
   - Process same meeting again through NoteCreator
   - Verify: uploadTextFile NOT called (noteFileName already set in mapping)
   - Verify: processNewMeetings returns { created: 0, skipped: 1 }

Use `beforeEach` to create fresh temp mapping store and mock client. Use `afterEach` to clean up temp files. Set jest timeout to 15000ms for rate limiting delays.
</action>
<verify>npx jest tests/integration/recurring-pipeline.test.ts --verbose passes all 3 tests</verify>
<done>3 integration tests pass: new recurring meeting pipeline, folder reuse for second instance, and note deduplication</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes all tests (existing + new integration tests)
- [ ] `npm run build` succeeds without errors
- [ ] Integration tests verify real service composition (not just individual mocks)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Recurring meeting pipeline tested end-to-end: folder creation → note upload → deduplication
  </success_criteria>

<output>
After completion, create `.planning/phases/05-integration/05-01-SUMMARY.md`
</output>

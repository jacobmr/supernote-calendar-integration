---
phase: 01-api-setup
plan: 01
type: execute
---

<objective>
Set up Google Calendar OAuth2 authentication and establish basic read access to upcoming meetings.

Purpose: Create the foundation for querying upcoming meetings from Google Calendar. OAuth2 establishes secure, user-authorized access without storing passwords.
Output: Working Node.js/TypeScript code that authenticates with Google Calendar and retrieves upcoming events
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech stack context:**
- Node.js/TypeScript project (assumed from Phase 1 setup)
- Google Calendar API v3 (REST)
- OAuth2 for user authentication

**Phase context:**
This is the first phase. No prior decisions constrain this plan. Phase 2 depends on calendar connectivity working.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Google Cloud project and enable Calendar API</name>
  <files>.env.example, config/google-api.ts</files>
  <action>
    1. Create Google Cloud project (free tier)
    2. Enable Google Calendar API
    3. Create OAuth2 credentials (Desktop/Node.js application)
    4. Document required scopes: https://www.googleapis.com/auth/calendar.readonly
    5. Create .env.example with required env vars: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI
    6. Create config/google-api.ts to initialize the Google Calendar client (use @googleapis/calendar v4)

    Why this approach: Desktop OAuth2 allows interactive browser-based auth during development. The unofficial Supernote API (Phase 2) will likely use API keys, so keeping auth patterns separate is cleaner.
  </action>
  <verify>
    - config/google-api.ts file exists
    - @googleapis/calendar package can be imported
    - Env vars documented in .env.example
  </verify>
  <done>
    Google Cloud project created, API enabled, OAuth credentials obtained, .env.example created, config file created
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement OAuth2 flow and get access token</name>
  <files>src/services/google-calendar.ts, src/utils/oauth-handler.ts</files>
  <action>
    Create OAuth2 authentication flow:
    1. Implement auth handler that opens browser for user login
    2. Set up local callback server to capture authorization code
    3. Exchange code for access token and refresh token
    4. Store tokens securely (use fs for now; Phase 6 will improve this for production)
    5. Implement token refresh logic (access tokens expire in 1 hour)

    Use @googleapis/calendar client library (official, actively maintained).
    Do NOT implement OAuth2 manually - use library's built-in oauth2 client.
    Store refresh token safely for scheduled job use (Phase 2).
  </action>
  <verify>
    - oauth-handler.ts exports authenticate() function
    - Tokens are retrieved and stored
    - console logs show successful auth (hide actual tokens in logs)
  </verify>
  <done>
    User can authenticate via browser, access token obtained, refresh token stored, auth handler callable from code
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify calendar read access with test query</name>
  <files>src/services/google-calendar.ts, tests/google-calendar.test.ts</files>
  <action>
    Implement and test basic calendar query:
    1. Create GoogleCalendarService class with getUpcomingEvents(timeMinutes) method
    2. Query next 30 days of events (use calendarId='primary')
    3. Parse response: title, start time, end time, attendees, description
    4. Write test that queries actual calendar (integration test, not mocked)
    5. Log results in readable format for verification

    This validates API access before moving to Phase 2. We'll use these events in the meeting detection engine.
  </action>
  <verify>
    npx jest tests/google-calendar.test.ts passes
    Actual calendar events are retrieved and logged
  </verify>
  <done>
    getUpcomingEvents() works, returns event objects with title/time/attendees, test passes
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Google Cloud project exists and Calendar API is enabled
- [ ] OAuth2 credentials are obtained and in .env.example
- [ ] Authentication succeeds without errors
- [ ] getUpcomingEvents() retrieves real calendar events
- [ ] No TypeScript errors in src/services/google-calendar.ts and src/utils/oauth-handler.ts
</verification>

<success_criteria>

- Google Cloud project configured with Calendar API enabled
- OAuth2 authentication flow implemented and tested manually
- Access token and refresh token obtained and stored
- getUpcomingEvents() successfully queries and parses calendar events
- Code builds without errors
- Integration test passes
  </success_criteria>

<output>
After completion, create `.planning/phases/01-api-setup/01-01-SUMMARY.md`:

# Phase 1 Plan 01: Google Calendar OAuth2 Setup Summary

**Google Calendar API authenticated and operational**

## Accomplishments

- Google Cloud project created with Calendar API enabled
- OAuth2 authentication flow implemented (browser-based)
- Access and refresh tokens obtained and stored
- getUpcomingEvents() method retrieves upcoming calendar events

## Files Created/Modified

- `config/google-api.ts` - Google Calendar client initialization
- `src/services/google-calendar.ts` - Calendar service and query methods
- `src/utils/oauth-handler.ts` - OAuth2 authentication flow
- `tests/google-calendar.test.ts` - Integration test for calendar access
- `.env.example` - Environment variables template

## Decisions Made

- Using official @googleapis/calendar library (actively maintained, well-documented)
- Desktop OAuth2 flow for interactive browser-based authentication during development
- Storing refresh token in local file for Phase 2 scheduled job

## Issues Encountered

None

## Next Step

Ready for plan 01-02-PLAN.md (Supernote API exploration)
</output>

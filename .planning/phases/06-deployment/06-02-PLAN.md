---
phase: 06-deployment
plan: 02
type: execute
---

<objective>
Add structured logging, error recovery, startup validation, and a README for operational readiness.

Purpose: Make the app observable, resilient to transient failures, and self-documenting for ongoing operation.
Output: Structured logging, startup credential validation, error recovery in scheduler, and comprehensive README.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deployment/06-01-SUMMARY.md

# Key files:
@src/index-scheduler.ts
@src/index-server.ts
@src/services/constants.ts
@docker-compose.yml
@.env.example

**Tech stack available:** Express, node-cron, Docker, start.sh
**Established patterns:** console.log with [Scheduler]/[Startup] prefixes, JSON file state, exponential backoff in constants.ts
**Constraining decisions:**
- No external logging libraries (keep dependencies minimal for self-hosted)
- JSON file state (no database)
- Single Docker container
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured logging and startup validation</name>
  <files>src/utils/logger.ts, src/index-scheduler.ts, src/index-server.ts</files>
  <action>
  1. Create `src/utils/logger.ts` — a thin wrapper around console.log:
     - Export `createLogger(prefix: string)` returning `{ info, warn, error, debug }` methods
     - Each method outputs: `[ISO-timestamp] [PREFIX] [LEVEL] message`
     - `debug` only logs when `LOG_LEVEL=debug` env var is set
     - Do NOT use winston, pino, or any external library — console is sufficient for a single-container self-hosted app

  2. Replace raw `console.log/warn/error` calls in `src/index-scheduler.ts` and `src/index-server.ts` with logger instances:
     - Scheduler: `const log = createLogger('Scheduler')`
     - Server: `const log = createLogger('Server')`
     - Keep existing log message content, just route through logger for consistent format

  3. Add startup validation to `src/index-scheduler.ts` (before scheduling cron):
     - Check required env vars: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
     - If missing: log error with specific missing var names and exit(1)
     - Check optional env vars: SUPERNOTE_EMAIL, SUPERNOTE_PASSWORD
     - If missing: log warning "Supernote credentials not set — folder/note creation disabled" and continue
     - Check data/ directory exists, create if not: `fs.mkdirSync('data', { recursive: true })`

  Do NOT validate credentials against APIs at startup — that would fail if APIs are temporarily down.
  Do NOT add log rotation or file-based logging — Docker handles log collection via stdout.
  </action>
  <verify>npm run build succeeds, npm test passes, grep "createLogger" src/utils/logger.ts</verify>
  <done>Logger utility created. All console.log replaced with structured logger. Startup validates required env vars and creates data/ directory.</done>
</task>

<task type="auto">
  <name>Task 2: Add error recovery and write README.md</name>
  <files>src/index-scheduler.ts, README.md</files>
  <action>
  1. Add error recovery in scheduler's cron job (`src/index-scheduler.ts`):
     - Wrap the Google Calendar query in a retry (use existing exponential backoff from constants.ts)
     - On 3 consecutive failures: log error with "Scheduler unhealthy: 3 consecutive failures" but keep running (next hour may succeed)
     - Track consecutive failure count in module scope, reset on success
     - Write failure status to scheduler-status.json so dashboard shows error state: `{ status: "error", lastError: "message", lastRun: "iso" }`

  2. Create `README.md` with these sections:
     - **What it does**: 2-3 sentence description (from PROJECT.md core value)
     - **Quick Start**: `cp .env.example .env`, fill in credentials, `docker compose up -d`
     - **Configuration**: Table of all env vars from .env.example with descriptions and defaults
     - **Google Calendar Setup**: Brief OAuth2 setup steps (create project, enable API, create credentials, run auth flow)
     - **Architecture**: Brief description — Express dashboard + hourly cron scheduler in single container, JSON file state in data/
     - **Manual Trigger**: `curl -X POST http://localhost:3000/api/trigger` with cookie auth note
     - **Troubleshooting**: Common issues (missing credentials, token expiry, Supernote API errors)

  Do NOT add badges, contributing guidelines, license sections, or changelog — this is a personal project.
  Keep README under 150 lines — concise and practical.
  </action>
  <verify>npm run build succeeds, npm test passes, test -f README.md, wc -l README.md shows under 150 lines</verify>
  <done>Scheduler has retry logic with failure tracking. README.md documents setup, config, and troubleshooting. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm test` passes all existing tests (105+)
- [ ] Logger utility used consistently across scheduler and server
- [ ] Startup validation catches missing Google credentials
- [ ] Scheduler tracks consecutive failures and writes error status
- [ ] README.md exists, is concise, and covers quick start through troubleshooting
- [ ] Phase 6 complete — project fully operational
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors or test regressions
- Structured logging throughout
- Startup validates environment
- Scheduler resilient to transient API failures
- README enables someone to set up from scratch
- Phase 6 complete — milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment/06-02-SUMMARY.md`

This is the final plan. Upon completion:
- Update STATE.md: Phase 6 complete, all phases done
- Update ROADMAP.md: Mark Phase 6 complete
- Project is operationally ready
</output>

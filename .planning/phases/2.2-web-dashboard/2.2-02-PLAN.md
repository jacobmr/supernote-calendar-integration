---
phase: 2.2-web-dashboard
plan: 02
type: execute
---

<objective>
Build dashboard UI, implement `/api/status` endpoint, and integrate Express server with existing scheduler in Docker.

Purpose: Complete the web dashboard by serving the monitoring dashboard after setup, exposing meeting state via API, and ensuring both the Express server and node-cron scheduler run together in production.

Output: Dashboard HTML template, `/api/status` API endpoint, updated Dockerfile running both services, and verified Docker integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
.planning/phases/2.2-web-dashboard/2.2-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2.2-web-dashboard/2.2-CONTEXT.md
@.planning/phases/2.2-web-dashboard/2.2-01-PLAN.md

**Building on Plan 01:**
- Express server already listening on port 3000
- Password protection middleware in place
- Setup form validates Supernote credentials
- Session authentication via httpOnly cookies

**Key files from previous phase:**
- `src/index-server.ts` - Main Express server (Plan 01)
- `src/middleware/auth.ts` - Auth middleware (Plan 01)
- `src/views/setup.html` - Setup form (Plan 01)
- `data/meeting-state.json` - Persisted meeting state (from Phase 2)
- `Dockerfile` - Needs update to run both Express + scheduler

**Existing scheduler context:**
- Node-cron runs hourly via `node dist/index-scheduler.js`
- StateManager persists meetings to `data/meeting-state.json`
- No modifications needed to scheduler logic

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard HTML template and `/api/status` endpoint</name>
  <files>src/views/dashboard.html, src/index-server.ts (add /api/status route)</files>
  <action>
  Create dashboard that displays meeting state summary:
  1. Dashboard HTML template (`src/views/dashboard.html`):
     - Clear heading: "Meeting Summary"
     - Display last run time: "Last updated: [timestamp]"
     - Show three metrics in readable format:
       - "New meetings: [count]"
       - "Changed meetings: [count]"
       - "Cancelled meetings: [count]"
     - Optional: Manual trigger button with confirmation: "Run scheduler now?" (POST `/api/trigger`)
     - Status indicator: "Status: Idle" or "Status: Running" (simple text, no animation)
     - Logout button linking to `/logout`
     - Simple styling: dark background, readable text, no framework

  2. `/api/status` GET endpoint in `src/index-server.ts`:
     - Read `data/meeting-state.json` if it exists
     - Return JSON with structure:
       ```json
       {
         "lastRun": "2026-03-12T14:32:00Z",
         "summary": {
           "new": 3,
           "changed": 1,
           "cancelled": 0
         },
         "status": "idle"
       }
       ```
     - If file doesn't exist yet: return default values with `summary: {new: 0, changed: 0, cancelled: 0}` and `lastRun: null`
     - This endpoint is protected by auth middleware

  3. `/logout` GET endpoint:
     - Clear session cookie
     - Redirect to `/login`

  **What to avoid:** Do NOT parse or modify meeting state—only read it. StateManager owns this file.

  </action>
  <verify>
  1. GET `/dashboard` with valid auth cookie returns 200 with HTML form
  2. GET `/api/status` returns JSON with correct structure
  3. Dashboard displays meeting counts correctly
  4. Logout button clears cookie and redirects to login
  5. Dashboard is readable, no console errors
  </verify>
  <done>
  - Dashboard HTML renders without errors
  - `/api/status` returns correct JSON from `data/meeting-state.json`
  - Logout functionality works
  - Dashboard shows meeting state summary clearly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Dockerfile to run Express server + scheduler together and add integration testing</name>
  <files>Dockerfile, docker-compose.yml (optional), package.json (add "start" script if needed)</files>
  <action>
  1. **Update Dockerfile** to run BOTH services:
     - Keep existing build steps (npm ci, npm run build, mkdir data)
     - Change CMD from just scheduler to run Express server and scheduler together
     - Use a simple approach: `npm run dev:all` or shell script that starts both processes
     - Express listens on port 3000, scheduler runs in background
     - Both should handle graceful shutdown
     - Example approach: Create `start-all.sh` script or use `concurrently` package

  2. **Add "start" script to package.json:**
     ```json
     "start": "node dist/index-server.js & node dist/index-scheduler.js"
     ```
     Or use a wrapper script if startup coordination needed.

  3. **Verify Docker behavior:**
     - Build image: `docker build -t supernote .`
     - Run container: `docker run -p 3000:3000 -e DASHBOARD_PASSWORD=testpass supernote`
     - Express server should respond to `/setup` within 5 seconds
     - Scheduler should log "Scheduler started" or similar
     - Container should persist state across restart (verify `data/meeting-state.json` exists)
     - Both processes should survive container lifecycle

  4. **Integration test checkpoint:**
     - [ ] Express server responds on port 3000
     - [ ] Scheduler logs indicate it's running
     - [ ] Can authenticate, access `/dashboard`
     - [ ] `/api/status` returns valid JSON
     - [ ] Container doesn't crash on startup

  **What to avoid:** Do NOT use complex orchestration (Kubernetes, etc.). Keep it simple for Railway deployment. Do NOT modify scheduler logic—only integrate it with the Express process.

  </action>
  <verify>
  1. `docker build` succeeds without errors
  2. `docker run` starts both Express and scheduler
  3. Port 3000 is accessible from host
  4. Express responds to `/login` with HTML form
  5. Dashboard loads and shows state
  6. Scheduler logs appear in docker logs
  7. `data/meeting-state.json` persists after container restart
  </verify>
  <done>
  - Dockerfile runs both Express server and scheduler
  - Docker image builds and runs successfully
  - Express server accessible on port 3000
  - Scheduler runs in background without blocking server
  - Meeting state persists in container volume/mounted directory
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Docker image builds without errors: `docker build -t supernote .`
- [ ] Container runs and exposes port 3000
- [ ] Can access `/setup` without auth (redirected from `/`)
- [ ] Can login with password and access `/dashboard`
- [ ] `/api/status` returns JSON with meeting counts
- [ ] Dashboard displays meeting state clearly
- [ ] Logout clears session and returns to login
- [ ] Scheduler runs in background (visible in logs)
- [ ] `data/meeting-state.json` persists across restart
- [ ] No TypeScript errors
- [ ] No console warnings or errors
</verification>

<success_criteria>

- All tasks completed
- Express server and scheduler run together in single container
- Dashboard shows meeting state summary correctly
- `/api/status` API endpoint returns valid JSON
- Docker integration working: image builds and runs with both services
- State persists across container restarts
- Clean, minimal HTML dashboard (no framework clutter)
- TypeScript compiles without errors
- Ready for Railway deployment

</success_criteria>

<output>
After completion, create `.planning/phases/2.2-web-dashboard/2.2-02-SUMMARY.md`:

# Phase 2.2 Plan 02: Dashboard UI & Docker Integration Summary

**[Substantive one-liner]**

## Accomplishments

- Dashboard HTML template displaying meeting state summary
- `/api/status` API endpoint reading meeting state from JSON
- Logout endpoint clearing session
- Dockerfile updated to run Express server and scheduler together
- Docker integration verified with both services running in single container
- Meeting state persistence across container restarts

## Files Created/Modified

- `src/views/dashboard.html` - Dashboard template
- `src/index-server.ts` - Added `/api/status` and `/logout` routes
- `Dockerfile` - Updated to run both Express server and scheduler
- `package.json` - Added startup script if needed

## Decisions Made

- Express server runs on port 3000, scheduler runs in background
- Dashboard reads from existing `data/meeting-state.json` (no modifications to state format)
- Simple startup approach: shell process background or npm script
- Dashboard shows only current state (no history or charts per user requirements)
- Clean HTML styling with no framework (minimal CSS)

## Issues Encountered

[None / describe any issues and how they were resolved]

## Next Phase Readiness

Ready for Phase 3: Folder organization system and note template generation.
Ready for Railway deployment: Both services containerized and state-persistent.

</output>
